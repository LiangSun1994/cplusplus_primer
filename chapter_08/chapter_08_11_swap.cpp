#include <iostream>
/*
template <typename T>
void swap(T& a, T& b);
int main(){
  int a = 1;
  int b = 2;
  swap(a,b);
  std::cout<<"a:"<<a<<std::endl;
  std::cout<<"b:"<<b<<std::endl;
}

template <typename T>
void swap(T& a, T& b){
  T temp;
  temp = a;
  a = b;
  b = temp;
}
 */