#include <iostream>
using namespace std;
/*
int sum(int * begin,int* end);
int main(){
  int arr[6] = {1,2,3,4,5,6};
  cout<<sum(arr,arr+3)<<endl;
}
int sum(int * begin,int * end){
  int res = 0;
  const int* p;
  //the pointer can can motify,but the value which the poiter point can not change.
  for(p = begin;p!=end;++p){
    res += *p;
  }
  return res;
}

 */